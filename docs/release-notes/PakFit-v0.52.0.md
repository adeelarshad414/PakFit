# PakFit v0.52.0 Release Notes

- Release evidence version 0.52.0 build 52.
- Adds PakFit-native agentic developer pipeline artifacts for specs-driven handoff.
- Adds `dev-start`, `dev-stop`, and `dev-restart` shell scripts plus PowerShell start/stop scripts.
- Adds VS Code tasks for native validation, release validation, screenshots, and demo recording.
- Adds machine-readable and human-readable SPEC_MAP files, deterministic demo scenario credentials, and a commands quick reference.
- Adds local setup guide in Markdown and Word DOCX form, with render-checked layout.
- Adds video script, voiceover guide, screenshot orchestration, demo clip orchestration, and demo assembly script.
- Adds release validation, CI, specs, and release evidence coverage for the agentic development pipeline gate.
